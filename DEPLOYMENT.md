# 🚀 部署指南

本文档介绍如何将 Tool.Eyou.ai 部署到各种平台。

## 📋 部署前准备

1. **修改默认密码**（重要！）
   - 编辑 `assets/js/auth.js`
   - 修改默认账号密码
   - 详见 `LOGIN_GUIDE.md`

2. **测试登录功能**
   - 确保登录页面正常工作
   - 测试所有工具的访问控制

3. **准备 Git 仓库**
   - 确保所有文件都已提交
   - 测试所有功能是否正常

4. **准备自定义域名**（可选）
   - 例如：tool.eyou.ai

---

## 1️⃣ Vercel 部署（推荐）

### 特点
- ✅ 完全免费
- ✅ 全球 CDN 加速
- ✅ 自动 HTTPS
- ✅ 支持自定义域名
- ✅ 自动部署（GitHub 集成）

### 部署步骤

1. **将项目推送到 GitHub**
   ```bash
   cd /Users/macmini/tool.eyou.ai
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <你的GitHub仓库地址>
   git push -u origin main
   ```

2. **登录 Vercel**
   - 访问 https://vercel.com/
   - 使用 GitHub 账号登录

3. **导入项目**
   - 点击 "New Project"
   - 选择您的 GitHub 仓库
   - 项目名称：tool-eyou-ai
   - 框架预设：选择 "Other"
   - 构建命令：留空
   - 输出目录：留空（根目录）
   - 点击 "Deploy"

4. **绑定自定义域名**（可选）
   - 部署成功后，进入项目设置
   - 选择 "Domains"
   - 添加您的域名：tool.eyou.ai
   - 按照提示配置 DNS 记录

5. **完成！**
   - 访问分配的域名或自定义域名
   - 每次推送到 GitHub，Vercel 会自动重新部署

---

## 2️⃣ Netlify 部署

### 特点
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 持续部署
- ✅ 表单处理功能

### 部署步骤

1. **登录 Netlify**
   - 访问 https://www.netlify.com/
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "New site from Git"
   - 选择 GitHub
   - 选择您的仓库
   - 构建设置：保持默认（留空）
   - 点击 "Deploy site"

3. **配置域名**
   - 进入 "Domain settings"
   - 添加自定义域名
   - 配置 DNS 记录

---

## 3️⃣ GitHub Pages 部署

### 特点
- ✅ 完全免费
- ✅ 与 GitHub 深度集成
- ✅ 简单易用

### 部署步骤

1. **推送到 GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <你的GitHub仓库地址>
   git push -u origin main
   ```

2. **启用 GitHub Pages**
   - 进入仓库设置（Settings）
   - 找到 "Pages" 部分
   - Source: 选择 "main" 分支
   - Folder: 选择 "/ (root)"
   - 点击 "Save"

3. **访问网站**
   - 等待几分钟
   - 访问：https://你的用户名.github.io/tool.eyou.ai/

4. **自定义域名**（可选）
   - 在 Pages 设置中添加自定义域名
   - 在域名提供商处配置 CNAME 记录

---

## 4️⃣ Cloudflare Pages 部署

### 特点
- ✅ 完全免费
- ✅ 全球 CDN
- ✅ 无限带宽
- ✅ 快速构建

### 部署步骤

1. **登录 Cloudflare Pages**
   - 访问 https://pages.cloudflare.com/
   - 登录您的 Cloudflare 账号

2. **创建项目**
   - 点击 "Create a project"
   - 连接 GitHub 账号
   - 选择仓库
   - 构建设置：保持默认
   - 点击 "Save and Deploy"

3. **配置域名**
   - 使用 Cloudflare 提供的域名或添加自定义域名

---

## 5️⃣ 自建服务器部署

### 使用 Nginx

1. **上传文件到服务器**
   ```bash
   scp -r /Users/macmini/tool.eyou.ai/* user@your-server:/var/www/tool.eyou.ai/
   ```

2. **配置 Nginx**
   ```nginx
   server {
       listen 80;
       server_name tool.eyou.ai;
       root /var/www/tool.eyou.ai;
       index index.html;

       location / {
           try_files $uri $uri/ =404;
       }

       # 启用 gzip 压缩
       gzip on;
       gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
   }
   ```

3. **配置 HTTPS**（使用 Let's Encrypt）
   ```bash
   sudo certbot --nginx -d tool.eyou.ai
   ```

4. **重启 Nginx**
   ```bash
   sudo systemctl restart nginx
   ```

---

## 📊 性能优化建议

1. **启用 CDN**
   - 所有平台都自带 CDN，无需额外配置

2. **压缩静态资源**
   - 已使用 CDN 链接的 Tailwind CSS
   - 考虑压缩自定义 JavaScript 文件

3. **缓存策略**
   - 静态资源设置长期缓存
   - HTML 文件设置短期缓存

4. **图片优化**
   - 使用 WebP 格式
   - 压缩图片大小

---

## 🔧 DNS 配置示例

### A 记录（指向 IP）
```
Type: A
Name: @
Value: 你的服务器IP
TTL: 3600
```

### CNAME 记录（指向其他域名）
```
Type: CNAME
Name: tool 或 www
Value: xxx.vercel.app 或其他平台提供的域名
TTL: 3600
```

---

## ✅ 部署后检查清单

- [ ] 首页能正常访问
- [ ] 所有工具链接正常
- [ ] HTML 预览工具功能正常
- [ ] 英语学习工具功能正常
- [ ] 移动端显示正常
- [ ] HTTPS 证书有效
- [ ] 页面加载速度快

---

## 📮 问题排查

### 1. 页面显示 404
- 检查文件路径是否正确
- 确保 index.html 在根目录

### 2. 样式不显示
- 检查 CDN 链接是否可访问
- 检查浏览器控制台错误信息

### 3. 功能不工作
- 检查 JavaScript 文件路径
- 查看浏览器控制台错误

### 4. 移动端显示异常
- 确保 viewport meta 标签正确
- 测试响应式布局

---

**祝部署顺利！** 🎉

如有问题，欢迎反馈。
